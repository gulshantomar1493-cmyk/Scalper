# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/

# Local configuration & secrets — NEVER commit (layer 2 of config order)
backend/config.yaml
.env

# Runtime settings — holds the Telegram bot token (secret), written by the UI
backend/runtime_settings.json
backend/runtime_settings.json.tmp

# Runtime artifacts
logs/
*.log

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node tooling (Playwright screenshot helper — dev only)
node_modules/
*.png
