# CLAUDE.md — Working Rules for MarketScalper

MarketScalper is a production-grade personal **market analysis & decision-support** platform.
It is NOT an automated trading platform, NOT an order-execution platform, NOT a broker terminal.
It generates trade recommendations; the owner always executes manually on their exchange and logs outcomes.
Owner: Gulshan. Solo-builder project.

## Frozen documents — never modify

- `docs/Architecture.md` — Blueprint **v1.2, FROZEN** (v1.2 = scope simplification: execution layer removed, engines untouched). Never redesign the architecture. No engines added or removed.
- `docs/IMPLEMENTATION_PLAN.md` — Implementation roadmap **v2.0, approved** (2026-07-14). Do not edit it without explicit owner instruction.

## Hard scope boundaries (v1.2 — never reintroduce)

No order placement · no trade/position management · no position sync · no broker auth · no order APIs · no kill switch/emergency flatten · no automated trailing/partials/time exits (management guidance is display-only text) · no PaperBroker/DeltaBroker/ExecutionProvider. Replay is for learning/validation/testing only — no replay execution.

## Process rules (non-negotiable)

1. Read all project files before doing anything.
2. Implement only approved modules — **one module (roadmap task) at a time**, in roadmap order.
3. Always explain the implementation plan and **wait for Gulshan's approval before coding**.
4. Never over-engineer. v1 scope: BTCUSDT + ETHUSDT only, 1m/5m, 3 strategies (S1/S2/S3), decision-support only. Single VPS, single process, single codebase; plain-Python provider interfaces wired in `main()` — no DI frameworks, no plugins, no microservices.
5. Always run tests after implementation.
6. Always update documentation after implementation.
7. `[DECISION]` roadmap tasks produce a short doc in `docs/decisions/` **before** their dependent code task. Decisions pin details the blueprint left open — they never change the architecture.
8. **Git milestone after every completed roadmap task** (rule set at P0.5): `git add .` → `git commit -m "P0.X: <short summary>"` → `git push`.

## Engineering disciplines (from Architecture §0 — locked)

1. **No repaint.** Every structural element computes only on confirmed closed candles. Store `ts` and `confirmed_ts`.
2. **Replay-first.** No engine is "done" until historical replay gives bit-identical output. Run the determinism harness after every engine change (same input candles → byte-identical output tables).
3. **No fake confidence %.** Score = weighted rule agreement (0.30 Structure / 0.30 Liquidity / 0.25 Volume / 0.15 Momentum), never probability. Two-tier display: Data Integrity + Signal Agreement.
4. **Validate before trust.** Minimum 200 logged recommendations with positive expectancy (after fees) before a strategy is marked TRUSTED. The platform never executes trades — there is no "live unlock".

## Stack (locked — see Architecture §2)

Python 3.12 + asyncio · FastAPI (WS + REST) · PostgreSQL 16 (append-only discipline) · Vanilla JS + TradingView Lightweight Charts v5 · FeedProvider abstraction with capability flags (BinanceFeed, ReplayFeed, optional DeltaFeed — public market data only; engines never see raw provider JSON) · single systemd process on a self-hosted Linux server (hosting provider = implementation decision, never named in docs) · pytest + pytest-asyncio.

## Repo layout

`backend/` providers, engines, recommendation layer & API · `frontend/` LWC v5 terminal · `database/` migrations · `deployment/` systemd/deploy · `scripts/` CI & tooling (determinism + import-boundary gates) · `tests/` pytest · `docs/` frozen docs + decisions.

## Conventions fixed at P0.2 (final)

Package name: `marketscalper` (stable, never rename) · deps in `pyproject.toml` use minimum compatible versions (`>=`), no strict pins unless absolutely necessary · config order (never change): `backend/config.example.yaml` → `backend/config.yaml` (git-ignored) → env vars override YAML · no secrets in git, ever · all logging via `logging.getLogger(__name__)`, UTC timestamps · `main()` in `marketscalper/main.py` is the only composition point · the project's single HTTP client is **aiohttp** (fixed at P0.11): used directly at call sites — no HTTP abstraction layers, no wrappers, no networking utilities.

## Current status

Phase P0 (Spine) in progress. Completed: P0.1, P0.2, P0.3 (test harness: `pytest` / `bash scripts/ci.sh`), P0.4 (decisions D1–D4: `docs/decisions/P0.4-engine-version-and-ops.md`), P0.5 (migration 001: candles + partition helper — `database/`), P0.6 (migration 002: pivots, levels, signals, recommendations, journal — data model complete), P0.7 (`marketscalper/db.py`: policy-free asyncpg access layer, structural append-only guard), P0.8 (DB unit tests: 21 tests vs local dev database via `MARKETSCALPER_DB_DSN`; skip without DSN, fail-with-instructions on missing schema, per-test rollback), P0.9 (`providers/base.py`: FeedProvider ABC + Capabilities + normalized Tick/Trade/BookTicker/Candle; `core/bus.py`: type-keyed EventBus, sequential deterministic delivery), P0.10 (`providers/binance.py`: BinanceFeed — combined WS aggTrade+kline_1m+bookTicker, normalize→publish, reconnect w/ backoff, heartbeat watchdog; historical fetch = NotImplementedError until P0.15/P0.16, `supports_historical_data=False` until then). P0.11 (decisions D5/D6 in `docs/decisions/P0.11-candle-authority-and-clock-sync.md`; `ClockOffsetSampler` in `providers/binance.py`; aiohttp added as the single HTTP client). P0.12 (`core/candle_builder.py`: Trade → deterministic 1m candles, publishes the existing `Candle` event — no wrapper event types, per owner clarification; closes only on next-bucket trade; out-of-order trades dropped with WARNING; no synthetic gap candles). P0.13 (5m aggregation inside the builder: internal fold of closed 1m candles, publish `Candle` tf='5m' at the A2 boundary `(bucket+1)%5==0`; partial windows across gaps discarded with WARNING). P0.14 (`core/reconciler.py`: KlineReconciler — explicit `on_built`/`on_reference` intakes wired at composition, pairing by (symbol, ts), D5 comparison o/h/l/c exact + volumes 0.1% relative, WARNING with both rows, `pairs_compared`/`mismatches` counters for the P0.28 gate; no expiry policy, unpaired stays pending). P0.15 (`fetch_historical_candles` implemented — REST /api/v3/klines via aiohttp, paginated, [start,end) ascending, `supports_historical_data=True`; gap-safe reconnect backfill: missing closed 1m candles fetched per symbol and published chronologically BEFORE live processing resumes; first connection / same-minute reconnect → no fetch; no dedup — duplicates stay with reconciliation). P0.16 (`bootstrap.py`: provider-blind one-time history load — capability check, end=floor(now), start=end−90d, partitions ensured first, two-phase validate-all-then-insert, <20-days refuse-to-run with nothing written; duplicates follow db.py plain-INSERT behavior). P0.17 (`core/candle_writer.py`: CandleWriter(bus, pool) — persists every bus Candle immediately via db.insert_candles; bus = truth-only by wiring: BinanceFeed's `on_reference_candle` callback routes live klines to reconciliation instead of the bus (None = standalone P0.10 behavior); insert failures caught/logged/counted, pipeline stays alive). P0.18 (coverage audit + 6 gap-fill tests: same-timestamp trades, interleaved multi-symbol 1m/5m determinism, cleanly-skipped 5m window, multi-symbol backfill ordering, backfill REST failure riding the existing reconnect loop, gap tracking advancing across reconnects; taker semantics documented as already covered — zero production changes, no bug found). P0.20 (`core/state.py`: SymbolState (last_candle_1m/last_candle_5m — only what the pipeline produces today) + StateStore (bus subscriber, `snapshot()` copies, `diff()` = changed-fields-since-last-call, collapsing)). P0.19 blocked until P0.24 per its own dependency line (needs ReplayFeed for the conformance suite). P0.21 (`api/app.py`: `create_app(bus, store, pool, api_token)` factory — GET /health open, GET /candles Bearer-auth history via db.select_candles (Decimal→float JSON), WS /ws?token= per D3 pushing `{candle, state_diff}` per closed candle, broadcast drops dead clients; construct StateStore BEFORE create_app so diffs contain the announced candle). P0.22 (frontend shell: `frontend/{index.html,styles.css,app.js}` — standalone static files, NO api/app.py changes per owner refinement; LWC v5 pinned @5.0.0, TradeOS tokens, empty chart surface, WS client `?token=` w/ 1s→30s reconnect backoff, status + last-event indicators; token in memory only). P0.23 (live 1m chart: REST bootstrap `setData` once, WS `series.update()` diff-only per §9; BTC/ETH switcher; 5m context strip as its own small chart; reconnect = reload history via REST, nothing more — thin client, no client-side gap/dedup/recovery/caching). P0.24 (`providers/replay.py`: ReplayFeed — Postgres 1m candles → identical normalized bus events at ×{1,10,60,max}, deterministic (ts, symbol) interleave; fully self-contained internal 5m fold (P0.24-only, never a shared component) with equivalence to the live builder enforced by the pipeline-identity test; capabilities orderbook=False/trades=False/historical=True/live=True; zero completed modules modified). P0.19 (shared FeedProvider conformance suite — six identical tests × BinanceFeed/ReplayFeed on one canonical dataset, provider-specific knowledge in fixtures only; + AST import-boundary gate: no package module outside `providers/` and `main.py` may import a concrete provider — runs inside the normal pytest step, ci.sh untouched). P0.25 (replay controls: four authenticated endpoints — /replay/start·stop·status·speeds — app owns one ReplayFeed at a time, concrete class injected via `create_app(replay_provider=...)` to keep the P0.19 import boundary; 409 on double start, idle stop = no-op, completion auto-idles via observe-then-latch; frontend: start/stop buttons + speed select + date-range inputs, replay candles flow over the existing WS payload; approved one-line ReplayFeed lifecycle fix — `connected=True` from task start). P0.26 (determinism harness v0: `tests/test_determinism.py` — same dataset replayed twice → sha256 over the canonical normalized event stream must be byte-identical; hash sensitivity self-test; runs in the normal pytest step, ci.sh untouched; grows to structure objects at P1.21, all objects P2.23, signals+recommendations P3.20). P0.27 (production composition: `main.py` = the composition root — env-driven launch settings per D3 (`MARKETSCALPER_API_TOKEN` required, refuse-to-start exit 2; HOST/PORT/FEED with defaults, `_FEEDS` plain selection map), wires EventBus → StateStore (before app) → CandleBuilder → CandleWriter → KlineReconciler (built candles via bus subscription, reference klines via BinanceFeed callback) → ClockOffsetSampler → create_app(ReplayFeed injected) → uvicorn; owns lifecycle per D2: partition ensure at startup + UTC-midnight rollover task; SIGTERM/SIGINT routed via loop.add_signal_handler to graceful uvicorn stop so the finally-cleanup (sampler/feed/pool) actually runs and exit code is 0 — without it uvicorn re-raises the captured signal and kills the process before cleanup; `deployment/marketscalper.service` (D4: Type=simple, app user, EnvironmentFile=/etc/marketscalper/env, Restart=always/5s), `deployment/env.example` (empty secrets — never in git), `deployment/deploy.sh` (6 steps, provider-agnostic: user+dirs, rsync code, venv `pip install -e`, env file fail-if-missing, migrations stay manual, systemd install+restart)). P0.28 (**P0 GATE PASSED 2026-07-17**, local acceptance, all owner-approved; criteria labeled AC1–AC4 — §6 owns "G" labels: AC1 live BTCUSDT 1m chart w/ WS closes + symbol switch + 5m strip ✓; AC2 zero candle mismatch — 28/28 reconciler pairs clean ✓; AC3 90-day replay — 129,599 candles ×max via UI, auto-idle ✓; AC4 `scripts/ci.sh` 199 passed / 1 skipped (documented POSIX-only SIGTERM test), conformance + determinism green ✓. Four gate-discovered fixes, each owner-approved: Windows signal guard in `main.py`; CORS middleware in `create_app` (D8); raw-trade-count `n_trades` semantics via aggTrade `f`/`l` (D5 clarification); startup partial-candle discard in the builder (D7 — owner product decision: correctness over completeness). Decisions + full gate record: `docs/decisions/P0.28-acceptance-gate.md`; closure report: `docs/P0-CLOSURE.md`). **Phase 0 complete — tag `v1.0.0-foundation`.** Phase 1 in progress (approved spec: `docs/P1-SPEC.md` — every open detail pinned or routed to the P1.3/P1.7/P1.12 decisions; execution order A: P1.1→P1.3→P1.2→P1.4). P1.1 (`engines/momentum.py`: `IncrementalATR` — skip-first-bar Wilder convention, SMA seed at candle period+1, RMA after, None until warm, O(1)/O(1), no wiring (nothing consumes ATR until P1.4/P1.5); validated by hand-computed vectors + an independent naive batch implementation — owner-approved substitution for a published vector; 14 tests in `tests/test_momentum.py`). P1.3 ([DECISION] D9 in `docs/decisions/P1.3-regime-constants.md`: regime constants as config — `regime.compression_ratio`=0.6, `regime.expansion_ratio`=1.5, `regime.median_window_bars`=240, `momentum.shift_accel_atr_ratio`=0.1 (ATR-scaled, explicitly uncalibrated, owned by P5.3); precedence expansion>coil>normal; regime None until ATR 1m+5m+median window all warm; replay logs one INFO distribution summary per symbol, counters only — no calibration/adaptive logic anywhere in P1–P4). Next: P1.2 (velocity/acceleration/momentum-shift/body-dominance in `engines/momentum.py`) — plan + approval first.
